# Dependencies
node_modules/
client/node_modules/

# Built client bundle (OPS-004 — built in Docker/CI, must not be committed)
client/build/

# Throwaway extraction/scan artifacts (OPS-004)
tmp_*

# Environment variables
.env
.env.local
.env.production

# Uploads (user files + accreditation docs) — not in git; live docs served from Cloudinary.
uploads/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Windows bat/vbs launchers
*.bat
*.vbs

# Windows reserved filenames
nul

# HTML export files (not needed for deployment)
BUILDEXEXPERTISE.html
BUILDEXEXPERTISE_files/

# Claude config
.claude/
